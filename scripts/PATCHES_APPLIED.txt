NeMo 2.5.2 Optional Dependencies Patches
=========================================

STATUS: ✓ PATCHES ALREADY APPLIED

The NeMo installation in this virtual environment has already been
patched to fix optional dependency import errors.

The following files have been modified:
  ✓ nemo/lightning/one_logger_callback.py
  ✓ nemo/lightning/callback_group.py
  ✓ nemo/collections/llm/gpt/data/api.py
  ✓ nemo/export/utils/model_loader.py
  ✓ nemo/collections/llm/api.py
  ✓ nemo/collections/llm/__init__.py

These patches make optional dependencies (nv_one_logger, nemo_run,
tensorstore) truly optional by wrapping imports in try/except blocks.

VERIFICATION:
-------------
To verify patches are working, run:

  source venv/bin/activate
  bash scripts/verify_nemo_fixes.sh

WHEN TO REAPPLY:
----------------
You only need to reapply patches if you:
  1. Reinstall NeMo (pip install --force-reinstall nemo-toolkit)
  2. Upgrade NeMo to a different version
  3. Delete and recreate the virtual environment

HOW TO REAPPLY:
---------------
If you need to reapply patches:
  1. See NEMO_FIXES.md for detailed patch instructions
  2. Apply patches manually to the 6 files listed above
  3. Run verify_nemo_fixes.sh to confirm

For more information, see:
  - NEMO_FIXES.md - Complete documentation of the issue and patches
  - scripts/verify_nemo_fixes.sh - Verification script

Generated: 2025-10-28
